{% extends 'base_user.html.twig' %}

{% block body %}
    <h1>User invite</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Invite" />
    {{ form_end(form) }}
{% endblock %}
