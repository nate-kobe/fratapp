{% extends 'base_user.html.twig' %}

{% block body %}
<br/>
    <div class="panel panel-default col-md-4 col-md-offset-4">
      <div class="panel-heading">
        <h3 class="panel-title">{{ user.firstName }} {{ user.lastName }}</h3>
      </div>
      <div class="panel-body">
        <b>Email</b> : {{ user.email }}<br/>
        <b>Permissions</b> : {% for group in user.securityGroups %}
                        <span class="label label-primary">{{ group.name }}</span>
                        {% endfor %}<br/>
        Compte crée le : {{ user.dtCreated|date('d-m-Y') }}<br/>
        <a href="{{ path('user_profile_edit') }}">Modifier</a> / <a href="#">Changer le mot de passe</a> 
      </div>
    </div>

    
{% endblock %}
